events{
 worker_connections 1024;
}

http{

  upstream backend {
    server 10.0.2.3;
    server 10.0.2.4;
  }

  server { # simple load balancing
    #listen          3000;

    location / {
      proxy_pass      http://backend;
    }
  }

}

